```java
result = []
def backtrack(路径, 选择列表)://选择列表是一些控制变量，控制你在路径选择时候该走哪条路的变量。
    if 满足结束条件:
        result.add(路径)  
        return
    
    for 选择 in 选择列表:
        做选择               
        backtrack(路径, 选择列表)
        撤销选择

```

